# notes.txt

- Bakery-MkDocs
    - .gitignore # See below
	- mkdocs.yml
	- docs/
        - images/
            - favicon.ico # Webpage Icon
		- index.md
		- home.md
		- cakes.md
		- pastries..md
		- contact.md
    - site/
        - 404.html
        - index.html
        - notes.txt
        - search.html
        - sitemap.xml
        - sitemap.xml.gz

.yml
theme:
  name: material
  palette: # Commented the background & root colors for this
    # Dark Mode
    - scheme: slate
      primary: teal
      accent: pink
      toggle:
        icon: material/weather-sunny
        name: Dark mode
    # Light Mode
    - scheme: default
      primary: grey
      accent: teal
      toggle:
        icon: material/weather-night
        name: Light mode

CSS colors
Charcoal: #4a4a4a
Cream:    #faf7f2
Teal:     #00b5bb


--Glass cards --
/*
Move the glass cards back into .hero-text, make this
full banner again using the .jpg
    background-image: url("../img/hero/background-img.jpg");
    position: relative;
    z-index: 1;
    color: white;
    background: rgba(75, 74, 74, 0.35);
    backdrop-filter: blur(10px);
    padding: 2rem 4rem;
    border-radius: 20px;
    box-shadow:
        0 10px 35px rgba(0,0,0,0.30);
    border: 1px solid rgba(255,255,255,0.15);
    max-width: 850px;
    width: min(90%, 850px);
*/

--Hero Button--
/*
.hero-button {
    background: white;
    color: black;
    padding: 10px 20px;
    border-radius: 8px;
    text-decoration: none;
}
*/

--Soft card look--
/* Add soft card look to sections */
/*.md-typeset h2 {
    margin-top: 3px;
    border-bottom: 2px solid #eee;
    padding-bottom: 10px;
    text-align: center;
}*/

----.-intro files----
/*
.bread-intro {
    text-align: center;
    max-width: 700px;
    margin: 0 auto 2rem auto;
    color: #6b5b53;
    font-style: italic;
}*/
/*
.gallery-intro {
    text-align: center;
    max-width: 700px;
    margin: 0 auto 2rem auto;

    color: #6b5b53;
    font-style: italic;
}*/
/*
.pastry-intro {
    text-align: center;
    max-width: 700px;
    margin: 0 auto 2rem auto;
    color: #6b5b53;
    font-style: italic;
}*/


--My theme--
Soft Highlight #555555
Subtle border  #3a3a3a
Background     #232323 --> charcoaln --> Replace #faf7f2
Cards          #2b2b2b --> card charcoal --> Replace #ffffff
Divider Lines  #404040 --> Replaces #e8ddd4
Accent         #00b5bb --> teal accent
Text           #f2f2f2 --> Replace #3f3f3f & #4a4a4a
Muted Text     #cfcfcf --> Replace #6b5b53
---------------------------------------
> `favicon.ico` - This icon is specific to the tab in your computer window.
        - its not on the web page

> .gitignore
    - In root of project!!
    - Used if a site like mkdocs literally regenerates their site over and over.
        You don't want to waste the time and memory saving something that'll be recreated anyways.
    - Add a line containing `site/` to your `.gitignore` file.
    - "PS > echo "site/ >> .gitignore" - inside `.gitignore`

> [!FAQ]- Are callouts foldable?
> Yes! In a foldable callout, the contents are hidden until it is expanded.



```markdown
> [!TIP] Callouts can have custom titles, which also supports **markdown**!
```


```markdown
.callout[data-callout="my-callout-type"] {
    --callout-color: 0, 0, 0;
    --callout-icon: icon-id;
    --callout-icon: '<svg>...custom svg...</svg>';
}
```

Sets and plugins can define custom callouts, too, or overwrite the default options. Callout types and icons are defined in CSS, where the color is an `r, g, b` tuple and the icon is the icon ID fromnipp any internally supported icon (like `lucide-info`). Alternatively, you can specify an SVG icon as a string.